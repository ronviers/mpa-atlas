# Engine readiness assessment (2026-05-31)

**Purpose.** Standing map of how close `character_engine` is to "ready," what is owed, and — the load-bearing
fact — **why most of what is owed cannot be closed by more work.** Written cold-readable as a handoff. No fork
of the engine (see `dissipative_rosetta_procedure.md` context): one engine, the tool is a projection.

## The one-line truth

**The engine's claim skeleton is substantially complete and synthetically vindicated. It is blocked from
crossing the commit line on *measured* data by a resource gap, not a work gap: there is no map-shaped real
substrate dataset we can run through the conform/inversion pipeline.** Adding the cascade apparatus to the
Character projection *raised* that data bar (multi-scale frustration + a perturbation protocol + non-artifact-
dominated Var(J)), past anything available. "If we had that data we could have closed nearly everything" is
correct. We do not have it, and we cannot manufacture it.

## What is DONE (so the engine is not mistaken for half-built)

The major claim-structure crossings landed (real-*emergent*, pre-conform — direct simulation / analytical
Jacobian on RPS + the homochiral triad):
- **Central Commitment** + **Two-Frame Construction** — promoted (`battery:sign-interior` RV1 on RPS).
- **character-primitives / the deformation chart** (gl(3,ℝ) Cartan) — promoted.
- **Homochirality** — promoted (named real-substrate stake, Frank/Kondepudi-class).
- **Amplitude-autonomy / the generative bet** — **closed this session**: continuous-amplitude autonomy is
  external-frame of necessity (supplied, not minted); chirality/topology mints. Derived two agreeing ways
  (spectral + the FDR definedness asymmetry recovering the mpa-legal rule as a theorem). Engine carries the
  Mintability corollary + test-validity guard.
- The dissipative-RG / two-mode-kernel / heat-tax / regime ontology / FDR two-frame machinery — dense, stated.

**So the engine matures now mostly by sharpening EDGES (boundaries, discriminators, over-reach), not by adding
territory.** Every recent error has been a boundary error: the amplitude close was a mint/supplied boundary;
the NFL tool over-maps were primitive-boundary failures. This is a good sign about distance-to-ready — the map
is drawn, the coastlines need sharpening.

## The ceiling: what is owed, and whether work can close it

Sorted by the only axis that matters now — **can we close it without map-shaped substrate data?**

### A. CANNOT be closed without real substrate data (the bulk — and the hard ceiling)
All of these are *runnable on synthetic now* and *gated on a real measured instance to promote*. The synthetic
pass is calibration, never vindication (gate `staked→promoted`(a)). **This is the resource wall.**
- The four batteries: `seam-collapse`, `wall-ladder` (real cascade + delay-Hopf), `dimensionless-self-probe`
  (second real substrate), `scale-covariant-circulation` (real OU/continuous instance).
- The staked trio: `gfdr-two-step`, `auto-tuning`, `memory-collapse` — each owes a real exercise / second instance.
- `consolidation-ascent` — needs a real **β_mem<1** substrate (the synthetic kernel is dynamically flat).
- `frustration-ascent` — amplitude leg closed; the **chirality-minting real-emergent joint cascade instance**
  remains. **This is the item the cascade apparatus most needs map-shaped data for, and the one most out of reach.**
- `thm9-crossover` / `thm6` / `thm7` — *forcing* the noise closure needs a substrate FDR.

**The blocker is upstream of all of these and is itself data-gated: the conform/inversion pipeline does not
work, and nothing post-conform is valid evidence yet (memory: `feedback_no_post_conform_evidence`). Even a
working pipeline needs data to consume. We have neither in usable form.**

### B. CAN be closed now — engine-internal, no substrate required (the real remaining engine work)
Small, and it does not change the ceiling — but it is honest engine maturation we control:
- **Two-frame exact magnitude identity** (`τ-window-reconciliation` + the Harada–Sasa velocity-form integral
  closing V_ext = ⟨σ⟩ = J·𝒜 numerically). Pure internal consistency. The lowest-hanging owed engine item.
- **Discriminator sharpening** (the tool-export surface): k_frust N≥3, ignition≠argmax, reservoir-must-be-
  sustained, advanced-primitive guard, grade-ceiling — *five sharpened this session via the NFL audits*. This
  is the one strand that genuinely advanced without substrate data, because the tool stresses boundaries.

## The reckoning (no softening)

1. **The synthetic ceiling is reached.** The engine has been vindicated *computationally* — repeatedly, on
   emergent substrates — and **never against the measured world**, because the measured world (in map-shaped
   form, post-cascade-apparatus) is not available to us. This is the documented honest scope, not a failure to
   fix by effort. It is the same wall as the parked syn3 north star: the real instance has always been the
   owed/parked thing.
2. **More substrate hunting is not the move.** The chiral-substrate hunt this campaign already ran to ground
   (library screened + ruled out; external hunt is the only path; no dataset meets the four criteria). Re-running
   it burns the time we don't have.
3. **What is deliverable WITHOUT the data** is real and worth doing in the time left:
   - **The tool (dissipative Rosetta)** — characterization/auditing across sciences. Explicitly NOT a
     substitute for real vindication; it *uses* the (synthetically-vindicated) engine as a coordinate system.
     Its export surface is closer to ready than the research engine because the NFL loop sharpened exactly the
     boundaries it needs.
   - **The Banach-cascade demonstration** — multiple Banach instances showing the cascade's *contours* (the
     envelope of substrate-property space), as demonstration/auditing, not as a real instance.
   - **The Tier-B engine-internal close** (the magnitude identity).
4. **If time runs out before data appears,** the correct end-state is: engine frozen at its synthetic ceiling
   with this assessment as the honest scope statement; the tool + demonstration as the deliverable downstream;
   the real-substrate crossings parked (not abandoned), exactly as syn3 is — awaiting data that is a resource
   acquisition, not a research step.

## Bottom line for "is the engine ready"

- **Ready as a synthetically-vindicated claim system + a tool coordinate-source:** yes, essentially — with the
  Tier-B magnitude identity as the one clean engine-internal close remaining, and the discriminators now sharp.
- **Ready as a world-vindicated theory:** no — and this is **not closeable by us without map-shaped substrate
  data.** That gap is a resource, not a task. Honest, not softened.
