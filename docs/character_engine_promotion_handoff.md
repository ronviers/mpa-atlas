# Character Deformation-Algebra → Engine — Promotion Handoff (2026-05-29)

**Read cold.** This is the active baton. It **supersedes** `syn3_alive_loop_handoff.md`, which is
**parked (long-term / out of reach for now)** — the real-substrate instance is not the near-term
move. The near-term, *reachable* work is to promote the **character deformation-algebra** into the
engine **synthetically**, by a forced-not-fitted derivation. That is discipline-legal because the
deformation-algebra is a **non-instance, structural** claim (its gate is a derivation, not a real
instance). The two relate cleanly: this synthetic structural law is the *foundation* the eventual
syn3 instance would later land on — doing it now does not abandon syn3; it earns the law syn3 needs.

---

## STATUS (one line)

Seven runnable specs characterize the triad's deformation generators + forced responses (all landing
on established closed forms, EP law verified at R²=1.0000); the task is to turn that empirical table
into a **derived, complete, forced** law and walk it the rung path to the engine.

## THE GOAL

Promote the character deformation-algebra — **the frustrable sub-topology (the A_CYC triad as
minimal carrier) + the complete generator basis + each generator's forced response** — to the
engine as an `OPERATORS`-adjacent section, via `battery:character-primitives`. **Hold the scope
split rigid:** the *structural law* ("the deformation space decomposes into these generators, each
forcing this response") promotes synthetically; the *coverage/life claim* ("real substrates' — and
the syn3 alive-loop's — character is a composition of these") stays the parked instance frontier.
Conflating them breaks the discipline.

## WHERE THINGS LIVE

- **Falsifier of record:** `mpa-atlas/docs/battery_character_primitives.md` — the spec to derive
  *against* (forced-not-fitted target, explicit kill/vindicate, the ready-to-plant compact frontier
  entry). Drafted, **not yet planted** in `mpa_frontier.md`.
- **Construction log (provenance/receipts):** `mpa-conform/docs/character_apparatus_log.md` — the
  nine-part record of how the apparatus was built, corrections kept in.
- **Apparatus:** `mpa-conform/scripts/{chiral_bonding, chiral_selffield, chiral_tilt, tilt_rescue,
  establishment_compare, character_primitives, squeeze_dynamics}.py`; figures in
  `mpa-conform/output/calibration/`.
- **Frontier:** `frustration-ascent` at `[sharpening]` (the structural side of the bet);
  `wall-as-type-boundary` sharpened. **Prior-art receipts already in place:** `nonhermitian-ep`,
  `adler-locking` (new), `bifurcation-normal-forms`, `nonreciprocal-transition`, `kuramoto`,
  `slaving`, `mz-projection`, `frank-autocatalysis`, `kondepudi`.

## THE RUNG PATH

`sharpening` (done — mechanism + specs) → `battery` (plant `battery:character-primitives`) →
`staked` (planted in receipts as the falsifier of record) → `promoted` (crosses the line). The
promotion requires **all four**, per the GATES non-instance branch:

- **(C) Completeness, derived** — *the load-bearing piece.* Prove the generator set is **exhaustive
  and closed** for the character-bearing operator (drift `gl(3)` = sym ⊕ antisym = damping ⊕
  splitting ⊕ chirality[mag+sign+orientation]; + drive; + nonlinearity; + the Floquet/parametric
  axis). A catalogue of "deformations we tried" does **not** clear this. Closure = law; list =
  frontier.
- **(R) Each response forced** — each generator's normal/coupling-pull derived as its canonical
  closed form. Mostly in hand (the establishment pass verified the headline forms; receipts in
  prior-art). Model-specific cells (the **~39° tilt node**) do **not** promote — they stay frontier.
- **(I5)** collapse (over-determined) + iff-chain (the basis *entailed*, not asserted) + flow (no
  inert constant in any response).
- **(G) goalpost-optic** — the basis must *shrink* the falsifiable surface (sharper predictions).

## NEXT-SESSION SINGLE MOVE

**The completeness/closure derivation (Part C).** It is the single piece that turns the table into
a law, and it is where the hard sub-question gets forced into the open: the **meta-scale symmetry
protecting the collective degeneracy.** As long as that symmetry is *hand-supplied* (the C3-covariant
coupling we imposed) it is **fitted and cannot promote.** Two honest routes, decide which during the
derivation:
- **(a) Derive it** — a forced structural result for *when/why* a protecting symmetry exists (the
  prize; the under-conceptualized side).
- **(b) Scope it conditional** — "*given* a substrate-supplied protecting symmetry, the generators
  force the collective degeneracy to lift into a protected meta-cycle" (honest,
  parasitic-on-substrate, promotable as a conditional law). Reachable now.

Optional precursor (locks the target before deriving): **plant the compact `battery:character-
primitives` entry** into `mpa_frontier.md`'s BATTERY section (text ready in the spec doc). Recommend
doing this first so the derivation has a fixed falsifier of record.

Resist scope creep beyond Part C — ship the closure attempt, look at whether it's a genuine closure
or still a catalogue, decide route (a)/(b) for the symmetry from what the derivation actually shows.

## THE THREE PIECES, GRADED

- **Frustrable sub-topology** (A_CYC triad, minimal carrier): easiest; mostly there
  (`banach_frustrated`, the `may-leonard` import). A clean structural claim.
- **Generator basis + forced responses:** the few-weeks core. Work = the completeness proof + the
  I5 audit; responses already pinned to canonical forms. The 39° node + non-uniform-squeeze
  secondary tongues stay frontier (model-specific) — honest, not a failure.
- **Meta-scale protecting symmetry:** the hard prize. Derive (a) or scope conditional (b). The one
  piece that, left hand-supplied, blocks promotion.

## LIVE FALSIFIERS (what stops promotion — pre-registered)

- **K1** basis not closed (a triad deformation that is not a composition of the generators).
- **K2** a claimed generator whose response is fitted / model-specific.
- **K3** the protecting symmetry admits neither derivation nor honest conditioning (only hand-
  supplied = fitted).
- **K4** I5 fail (inert constant; or basis asserted not entailed).
A clean K1/K3 outcome is a real result: the algebra stays a frontier catalogue / parasitic on a
supplied symmetry. Be prepared for it.

## DISCIPLINE

Forced-not-fitted means *derivation, not tabulation* — adding more cases does not earn the engine;
proving closure does. Keep the structural/coverage split rigid (do **not** smuggle syn3 or any
real-substrate claim into the structural promotion). Each promoted line gets a line-keyed receipt
in `mpa_receipts_engine.md` *in the same session* (the establishment imports supply most of them;
the new receipt is the closure derivation). Strict thresholds, low commitment; a piece that can't
be forced stays frontier. Framework edits held for review unless told otherwise; auto-push only
committed work.

## DECISIONS FOR RON

1. **Plant the battery entry first** (lock the target), or derive first and plant once the closure
   shape is known?
2. **Symmetry route:** aim for the derivation (a, the prize) from the start, or bank the conditional
   law (b) first and pursue (a) after?
3. **Confirm syn3 is parked** as the long-term instance frontier (not abandoned) — and that the
   structural law is explicitly its foundation, so the relationship is recorded, not lost.
4. **Engine landing site:** new `OPERATORS`-adjacent section "deformation generators" — confirm the
   name/placement before promotion.
