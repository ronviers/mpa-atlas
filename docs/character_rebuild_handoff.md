# Character — current state & next (post-rebuild)

> **STATUS (2026-06-02): the scientific-register rebuild LANDED and is committed + pushed.**
> `github.com/ronviers/character-framework` `main` @ `51be155`. The trio plan is retired —
> `character_manifold` and `character_composite` were *folded into one unified `character.md`*,
> not kept as separate docs. This file is the slim current-state pointer; the live "what's next"
> is the maturity ledger `character-framework/framework/character_frontier.md`.

## Home

`character-framework/framework/` is the single source of truth (public **and** dev). **All
`mpa-*` repos are legacy/frozen** (`mpa-atlas` at tag `character-v0.1`). Don't edit the engine
going forward; `mpa-conform` reads the frozen engine during the transition, then repoints to the
scientific SoT once it's operationally complete enough.

## The corpus (all in scientific register)

- **`character.md`** — one unified paper, three movements: the point (one NESS), the space (the
  morphospace `ℭ`), the closure (`⊗`). Carries the restored operational math (two-mode kernel,
  capacity, dual ledger), the two-kinds protected bit (structurally stored vs spontaneously
  frozen), the multi-face boundary of the open operating interior, and the
  precision/protection/identity measurement layers.
- **Four-axis support:** `character_prior_art.md` (imports, each named) · `character_receipts.md`
  (derivations) · `character_frontier.md` (maturity ledger). Plus depth companions
  `character_fdr_treatment.md` and `character_translation_method.md`. Plus `experiments/`.

## Register rules (one register: scientific)

Standard units (`bits = ln 2 nats`); every import named to source; substrate-general (no
anthropomorphism — basin escape for a driven NESS, not a theory of selfhood); honest scope
stated as plain fact; **avoid the word "emergence"**; no development-discipline scaffolding in
the public docs (falsifiers/limits consolidated in a closing passage).

## Open (from `character.md` §Status + the frontier)

- the **β data-collapse** test against measured laboratory data — not yet run;
- the **identity-survival barrier** — the basin-escape quasipotential — uncomputed on a real
  chiral substrate (homochiral racemic saddle is the natural first instance);
- the tangent-space identification instanced only at `n = 3`;
- coupled-NESS thermodynamics of two interacting characters;
- the **`conform` repoint** to the scientific SoT;
- the **self-referential closure** and the **alive loop** — parked (the program's north star).

## Publishing

Zenodo retracted (~2026-05-29); re-publication needs Ron's explicit go.
