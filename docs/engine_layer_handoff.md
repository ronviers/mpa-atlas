# Engine §0 layer — DONE (2026-05-31)

**This handoff is retired.** The task it described — adding the first-contact `§0 — Objects & Definitions` layer
to the engine — is **complete**. The layer is now in `framework/character_engine.md` (top, above PARAMETERS): the
settled identity line, the load-bearing nouns defined inline (character, ch, chit, NESS, drive/dissipation,
Banach reference, holding, trail, coherence, k_frust, gauge, metabolize), and the one-line lineage note.

It was written **inline by the integrator**, not delegated — two delegated attempts each produced an elaborated
*document* instead of an engine edit (the handoff was a gravity well: a model handed a rich doc pours content
into it rather than editing a different file). Lesson banked for future canonical-prose tasks: **edit the target
file directly; do not hand a fresh session a document to "build from."**

## Rationale of record (why the layer exists, what it contains)
- `engine_definitional_gap_audit.md` — the per-term reasoning + severity sort the §0 layer distills.
- `naming_decision.md` — the settled identity line + the name-as-capstone decision.
- `vocabulary_refactor_spec.md` — the larger rewrite sequence §0 is the first content step of.
- `engine_readiness_assessment.md` — the honest scope the §0 definitions stay within.

## Still owed (NOT part of §0 — future, deliberate)
- **The public-facing engine rewrite proper** — compelling prose throughout (§0 is just its first step).
- **The telos preamble** — the deep *why* (parked alive-loop north star), if/when Ron wants it.
- **The capstone name** — the framework's final name, chosen WITH Ron as the rewrite's last act
  (`naming_decision.md` Decision 3). MPA stays the working/citation handle until then.
- **Confirm the MPC→MPA etymology** with Ron — the §0 lineage note says "Metastable Propositional Calculus"
  but states it plainly without the acronym mechanics; if Ron wants the Calculus→Algebra detail it can be added,
  but it should be confirmed first (inferred, not verified in canon).
