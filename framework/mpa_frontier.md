# MPA — FRONTIER (maturity ledger)

The fourth axis. The other three say *what is claimed* (engine), *how it is derived* (receipts), *what is borrowed* (prior-art). This one says **how settled each thing is, and exactly what would move it.** It is the single source of truth for everything not yet load-bearing. Its job is not to feed the core — it is to keep provisional work **out** of the core until it earns in. Structure here is quarantine, not on-ramp.

The value of the engine is that everything in it is committed. That guarantee survives only if the line below is strict.

---

## GATES — the state machine (binding)

Five rungs and a hard line. An item occupies **exactly one rung at one time**; moving rungs is an explicit event with a **recorded trigger** (no session-lore promotions).

```
steeping ──▶ sharpening ──▶ battery ──▶ staked ──╮
   │             │             │           │      │  COMMIT LINE
   ▼             ▼             ▼           ▼      ▼  ───────────────
 discarded (tombstone) at any rung         promoted → engine / receipts / prior-art
```

**Promotion gates** (the condition to cross each boundary — strict, and these are the framework's own rules, not new ones):
- `steeping → sharpening` — the speculation acquires *either* a concrete mechanism *or* a way to test it (a named owed item, or a candidate spec). Pure intuition with neither stays steeping.
- `sharpening → battery` — a runnable falsifier spec exists: finite-operating-point, a **forced-not-fitted** target, and explicit kill **and** vindicate conditions.
- `battery → staked` — the spec is planted in the receipts as a falsifier of record (the `staked` tag), un-instanced by design.
- `staked → promoted` **(crosses the line)** — requires *all* of: (a) a **real cross-substrate instance**, not synthetic — a synthetic pass is calibration, never vindication; OR for a non-instance claim, a **forced-not-fitted derivation**; (b) **goalpost-optic** — the move shrinks the falsifiable surface, never enlarges it; (c) for a refinement that *defends* an existing claim rather than *adds* one, a **second independent instance**. The promotion records what crossed, the evidence, and which core doc absorbed it.

**Demotion / kill gates:**
- Any item whose promotion gate is disproven → `discarded`, with a one-line **tombstone** (what it was, why it died) so it is never silently re-explored.
- A `staked` falsifier that **fires** does not just demote — it **kills the core claim it defended**; the kill propagates into engine/receipts. Falsifiers are load-bearing in both directions.

**Schema (mandatory).** Every register entry carries five fields. An entry missing any field is malformed and may not be added:
`key` **[rung]** — *verdict* (current belief, one line) · **↑** promote-gate · **✗** kill-condition · **→** core target (the claim it becomes / the falsifier it operationalizes), or `pure-exploration` if it has no target yet (permitted **only** at the steeping rung).

---

## INVARIANTS — auditable (the discipline, mechanized)

- **I1 — bridge integrity.** Every `staked` entry in the receipts ↔ exactly one frontier entry at the `[staked]` rung. The staked rung is the only place an item is simultaneously in the core and on the frontier; it must reconcile both ways.
- **I2 — no orphans.** Every frontier entry has a `→` core target, *or* is flagged `pure-exploration` (steeping rung only). Nothing floats.
- **I3 — purity of the core.** Nothing above the commit line lives in a working file; nothing below it lives in engine/receipts/prior-art. The core contains only what crossed.
- **I4 — recorded triggers.** No rung change without a logged trigger meeting the named gate. Absence of a trigger = the move did not happen.
- **I5 — self-application (recursion, layer 1).** MPA is itself a dissipative register-system, so its own claims face the filters it applies to substrates. Every `staked`/`promoted` claim must pass all three: **collapse** — it is over-determined (a binding or falsifier it can be checked against; never a single-register or definitional identity); **iff-chain** — its architecture is a stated entailment, not an asserted axiom; **flow** — every dynamical quantity in it moves with the operating point, no inert constant (the mpa-legal audit, receipts §CORRECTIONS). A claim failing any of the three sits in MPA's own $r$-regime (metaphor) and cannot stake. This is the audit turned inward; *generative* self-application (layer 2 — MPA minting its own register) is the open bet, gated at `flow-resident-number` / `frustration-ascent`.

- **I6 — no silently-committed conjectures.** A core claim carrying a falsifier must be in one of three states: its falsifier has a **recorded run** (a real instance or an apparatus), or it is a **forced-not-fitted derivation**, or it is explicitly **`staked`** (the bridge rung, un-instanced by design — I1). A claim with an un-run falsifier and no derivation that is *neither* `staked` *nor* on the frontier is malformed — a conjecture committed as if settled — and demotes to the frontier. (This is the gate a boundary-import claim slips when no interior apparatus exists to run it: the antidote is that behavior at a degenerate point is an *entailed theorem of the interior*, never an un-run import asserted at the limit — engine membership self-test (iii).)

(I1, I2, the I5 three-filter audit, and I6 are checkable mechanically — I1/I2/I6 against the receipts (every committed core falsifier ↔ a recorded run, a derivation, or a `staked` tag), I5 per staked claim — the same way the prior-art map is.)

---

## REGISTER

### STEEPING — speculation (deduplicated from the forked steeping notes; no commitment)

* `flow-resident-number` **[steeping]** — *verdict:* candidate number-type whose identity is a non-terminating trajectory under the meta-ledger flow (chit-as-orbit-affinity, the slow-manifold trace $P_s,\alpha_s$, $\tau_R$, the running-coupling $\beta$-functions); value-readings are static projections. · **↑** a type definition + composition algebra under the meta-ledger flow + a natural metric (candidate: trail-class $\rho$). · **✗** no closed composition algebra exists. · **→** `pure-exploration` (would make asymptotic closure its carrier rather than an independent invariant). **Recursion (layer 2, I5):** the *algebraic* side of the generative-recursion bet, paired with `frustration-ascent` (structural side). A closed composition algebra here = MPA minting its own register under the meta-ledger flow instead of importing one (parasitic→generative); the two-mode kernel is the recursion's fixed point. If the ✗ fires, generative self-application is vacuous and only the layer-1 self-audit survives.
* `fractional-operator-algebra` **[steeping]** — *verdict:* strongest steeping candidate; $\{C_\beta,S_\beta,K_\beta,R_\beta\}$ on memory regimes $\beta_{\text{mem}}\in(0,1]$, recovering $\{C,S,K,R\}$ at $\beta{=}1$; the algebra of *coupled* fractional systems is genuinely open. · **↑** existence of a closed algebra at finite $\beta$ + a deformation-calculus analog (Thm 6/7/9 counterparts) + boundary rules at $\beta\to1^-$. · **✗** no closed algebra at finite $\beta$. · **→** a memory-regime operator layer paralleling engine `OPERATORS` (seven-register universality would carry it).
* `two-reframe-parallel` **[steeping]** — *verdict:* Boolean $=D\to\infty$ limit (static reframe) and classical-chaos $=\varepsilon\to1^-$ limit (dynamical reframe) share architecture; watch for a third axis. · **↑** a third concrete reframe axis, instanced. · **✗** the parallel proves merely cosmetic. · **→** `pure-exploration`.
* `secondary-regime-operators` **[steeping]** — *verdict:* damping (under/crit/over), phase-locking (locked/drift/chimera), meta-ledger ($\varepsilon$ conv/crit/div) as further operator-algebra candidates; memory regimes are the cleanest first instance. · **↑** a closed algebra on any one regime trichotomy. · **✗** regimes do not support closed composition. · **→** `pure-exploration`.
* `post-zero-trail-universality` **[steeping]** — *verdict:* candidate seam mechanism cross-pollinated from the sharpening journal; flagged "something to consider," no mechanism yet. · **↑** a concrete mechanism connecting it to the seam-width law. · **✗** no link to seam universality survives. · **→** `pure-exploration` (would feed `thm9-crossover`).
* `scale-covariant-circulation` **[steeping]** — *verdict:* a new two-frame disagreement on the **scale axis** (distinct from external-$X$ vs self-probe-$\mathcal{T}$, which read at one scale): the *same* triad circulation read at two scales — local/fine vs global/coarse via the legitimate RG map — splits into a scale-**invariant** affinity $\mathcal{A}$ (sign *and* value, the topological bit) and a scale-**covariant** magnitude $J$ (renormalizes by the eliminated-mode timescale ratio). $\mathcal{A}$-invariance is *derived, not asserted*: under the legitimate map each cycle edge's forward/backward rates are dressed by a **common** conditional-stationary factor that cancels in the ratio $\prod_+k/\prod_-k$ but survives in the absolute rate. **Legitimate map** (crux settled 2026-05-27) = adiabatic Mori–Zwanzig $\Pi_{\text{slow}}$ under **(A)** local detailed balance on the eliminated subspace ∧ **(B)** $b_1$-preservation (temporal version: $\tau_{\text{obs}}\ge$ cycle period). Imports: `pa:esposito-coarse-graining` (time-scale-separation EP-preservation, owed), Betti/cycle-rank, $\Pi_{\text{slow}}$. · **↑** a runnable spec on a clean $\mathcal{A}$-bearing substrate (`banach_frustrated` embedded with fast bystanders for the spatial $\Pi_{\text{slow}}$, or its $\tau_{\text{obs}}$-window axis reusing the v13 camera): sign($\mathcal{A}$) preserved + $J$ renormalizing as predicted, both illegitimate violations pre-excluded → `battery`. · **✗** under a map satisfying **A∧B** (or the temporal floor), sign($\mathcal{A}$) flips or $\mathcal{A}$ erases — the topological bit migrates with scale. (Pre-classified **not** kills: A-fail = hidden-EP loss; B-fail = loop contraction = rewiring per the Central Commitment's edge-deletion clause.) · **→** engine `SCALE-RELATIVITY` ($k_{\text{frust}}$ non-migration) + `COMPRESSION` (RG flow, $k_{\text{frust}}$ invariant); receipts §RG closure; sharpens `consolidation-ascent`'s RG-invariant $b_1$ leg.
* `palm-self-probe` **[steeping]** — *verdict:* the two-frame iff-chain re-projected into the point-process/queueing domain — the **time-stationary** average (external frame) and the **Palm / event-conditioned** average (self-probe; the system's own event stream is its intrinsic clock) are an inequivalent pair tied by Palm inversion (`pa:palm-calculus`, owed). **PASTA** (Poisson arrivals; `pa:pasta`, owed) is the $X\equiv1$ degenerate case — consistent with v12's reversible M/M/1 reading $X{=}1$. On a 3-station **non-reversible cyclic** queueing network (Jackson/Kelly; the queueing analog of `banach_frustrated`) the chain reinstantiates: self-probe defined $\Leftrightarrow$ net customer circulation $J\ne0\Leftrightarrow$ routing-affinity $\mathcal{A}\ne0$ (Kolmogorov criterion on the routing matrix fails, `pa:kelly-reversibility`, owed) $\Leftrightarrow$ a directed routing cycle (the triad, new domain). Would hand Cat-9 a live falsifier replacing the parked $\alpha_s{=}\tfrac12$ category error. · **↑** resolve the **self-probe-current crux** — the protected current is the *gauge-irremovable routing-cycle affinity*, **not** the always-present arrival rate (M/G/1 shows PASTA-coincidence *without* reversibility, so frame-coincidence $\ne$ detailed balance) — + a candidate spec on a 3-station cyclic network → `sharpening`. · **✗** a non-reversible cyclic queueing NESS where Palm and time-average frames **agree** on regime despite $\mathcal{A}\ne0$ (definedness-asymmetry collapse), or **disagree** with no directed routing cycle (iff-chain break in the new domain). · **→** engine `TWO-FRAME CONSTRUCTION` (new projection of the iff-chain) + `ADOPTION` queueing row (replacing the parked $\alpha_s{=}\tfrac12$ tooth); receipts §gFDR two-frame.
* `epsilon-hub-transport` **[steeping]** — *verdict:* candidate transport law on the distance-to-Wall parameter $\varepsilon$ (method: `transport_law_steps.md`) — one $\varepsilon$ read in four registers via fixed maps: FDR aging $\beta_{\text{mem}}\approx1-\varepsilon$ (P1), heat-tax $\alpha(\varepsilon)=\alpha_0(1-\varepsilon)$ (§6), avalanche branching $\to1$ + SOC $\tau\approx3/2$ at $\varepsilon{=}1$ (`pa:soc`), CTRW critical length $\ell_c(\beta_{\text{mem}})$ (`pa:ctrw`). Degenerate at $\varepsilon{=}0$ (equilibrium) and $\varepsilon{=}1$ (Wall: all $\to1$/diverge); content lives in the open interior. · **↑** a runnable collapse-test spec on one substrate measuring $\ge2$ registers and checking they invert to a common $\varepsilon$ through the maps (forced-not-fitted) → `sharpening`. · **✗** the maps are mutually inconsistent so no single $\varepsilon$ can parametrize them, OR two registers are tied definitionally in the source literature (Step-2 fail — no new content). · **→** engine consolidated falsifiers `Memory exponent` (P1) + `SOC`; receipts §6 heat-tax, §18 branching; operationalized by `transport_law_steps.md`.
* `chit-hub-transport` **[steeping]** — *verdict:* candidate transport law on the load parameter chit$\,=-\ln\rho$ (method: `transport_law_steps.md`) — one chit read in three registers: queue load chit$\,=-\ln\rho$ (engine `ADOPTION` queueing), relaxation-oscillation $Q(\text{chit})$ non-monotonic, peak at chit$=1\,$ch (§13), phase-lock suppression $K_{AB}\propto(1+4Q^2)^{-1/2}$ (§15). Degenerate at chit$\to0$ and chit$\to\infty$ (both $Q\to0$, overdamped both ends); signal only in the mid-chit band. RO and phase-locking are both already on the testbeds, so the test side is near-met — the open piece is the *binding* (one chit reads all three). · **↑** a runnable collapse-test spec: measure the RO band ($Q$) and phase-lock suppression on one substrate, invert both to a common chit (forced-not-fitted), predict the load $\rho$ → `sharpening`. · **✗** the RO-inferred chit and phase-lock-inferred chit cannot agree by construction, OR a Step-2 fail. · **→** engine `ADOPTION` queueing + §13/§15 claims; receipts §13 stability, §15 phase-locking; operationalized by `transport_law_steps.md`.

### SHARPENING — active work on a named owed item (the lab journal's *current verdict*, lifted out of its chronological log)

* `thm9-crossover` **[sharpening]** — *verdict:* Thm 9 is **not** a $1/D$ coefficient series but a singular crossover scaling function (width exponent $\alpha$ + profile class); **fitted-not-forced** as it stands — $\alpha$ swings with the input noise closure across a closure sweep. · **↑** the noise law derived from a substrate FDR forces $\alpha$ (fitted→forced), validated on a non-toy substrate by scaling collapse. · **✗** no stable $\alpha$, no collapse, or multiple incompatible collapse families. · **→** receipts §Deformation calculus Thm 9 (`bespoke`, bounds-only); operationalized by `battery:seam-collapse`.
* `thm6-bulk-series` **[sharpening]** — *verdict:* looks like a genuine $D^{-1}$ bulk series ($p{=}1$, trilinear in shears × overlaps × merge-derivative), conditional on smooth-merge closures. · **↑** the closures derived, not assumed. · **✗** the series fails off the fitted closures. · **→** receipts §Deformation calculus Thm 6.
* `thm7-bracket` **[sharpening]** — *verdict:* hinges on whether $S$'s $\max(0,\gamma)$ threshold is hard or $1/D$-soft (asymptotic closure says soft → dissolves the apparent global-bracket obstruction). · **↑** the survivability-threshold softening law pinned. · **✗** a hard threshold confirmed (obstruction real). · **→** receipts §Deformation calculus Thm 7.
* `two-faces-boundary` **[sharpening]** — *verdict:* the amplitude and sign-topological faces (independent in the bulk) are conjectured **corresponded at their boundaries**; a candidate wall-crossing law exists ($\mathrm{spec}(M)$ complex pair $\Leftrightarrow\mathcal{A}\ne0$; boundary $=$ eigenvalue coalescence). · **↑** index-grade criteria met — exact pairing $\nu=F(\Phi)$; $\Delta\nu\ne0\Leftrightarrow\Phi$ singular; transversality/reconstruction. · **✗** the faces prove independent at the boundary too (independence is then global, not just bulk). · **→** engine open item (two-faces boundary correspondence) — a *refinement* of the independence commitment, not a break.
* `dimensionless-substrate` **[sharpening]** — *verdict:* the non-dimensionalization standard is now fixed in `mpa_units.md` — the **ch** ($\equiv\ln2$ nats) is the **forced** unit (Q-peak at chit$=1\,\text{ch}$, robust across RO forms), conventions resolved ($\beta_{\text{mem}}{=}1$ primary, $\alpha_{\sigma,0}=$ conductivity unit); residual is instancing the dimensionless self-probe $T$ on a real frustrated-and-noisy substrate. **trigger (I4):** `mpa_units.md` written; the ch forced by the Q-peak; conventions fixed — steeping→sharpening. · **↑** a frustrated-and-noisy Banach extension hosting the dimensionless self-probe $T$ at the standard operating points → `battery`. · **✗** the dimensionless self-probe fails to close on a real frustrated substrate. · **→** engine `FRAMES` self-probe ruler; standard in `mpa_units.md` (ties to `battery:sign-interior`).
* `τ-window-reconciliation` **[sharpening]** — *verdict:* the TUR-tightness $\mathcal{T}$ has two formulations in working notes — a $\tau$-absorbed form (the canonical $\mathcal{T}=\langle\sigma\rangle\mathrm{Var}(J)/(2\langle J\rangle^2)$ used in engine `FRAMES`) and a $\tau$-explicit form (matched current/EP averaging window, e.g. $\mathcal{T}(\tau)=\langle\sigma\rangle(\tau)\mathrm{Var}(J(\tau))/(2\langle J(\tau)\rangle^2)$) — that must reconcile to one form, with the absorbed form taking $\tau$ as the NESS natural averaging time. **trigger (I4):** discrepancy surfaced in the FDR-treatment sync pass; flagged as the open numerical-identity refinement to §gFDR two-frame. · **↑** derive one form from the other (or show the choice is conventional and state the convention explicitly, $\tau$ = NESS natural averaging window) — closing the velocity-form Harada–Sasa magnitude identity $V_{\text{ext}}=\langle\sigma\rangle=J\cdot\mathcal{A}$ numerically. · **✗** the two formulations produce different numerical regime verdicts on the same substrate at a fixed operating point. · **→** receipts §gFDR two-frame; engine `FRAMES`; `mpa_fdr_treatment.md` §6 Open.* `consolidation-ascent` **[sharpening]** — *verdict:* one mechanism, three faces — the **same heat-tax** that forces *endogenous* offline drive-withdrawal (a duty cycle: maintenance $\langle\sigma\rangle$ outruns online dissipation toward the Wall [§6 heat-tax + FRAYING] **and** the compression operator $\mathcal{C}$ contends with active holding for the rate-limited server [§22 priority]) also forces the **tower ascent that *is* consolidation**: nesting $=$ recursive character (RG type-identity, §6.5 — the coarse-grained level is again the two-mode kernel, same $c/s/r$ + gFDR), the amplitude channel compressing under $\varepsilon<1$ toward $\mathcal{M}_2$ while the topological $b_1$ threads up RG-invariant ($k_{\text{frust}}$ does not migrate); recurse-vs-forget set by the $\langle\sigma\rangle\ge-\Delta I$ exchange (§17 — pay to nest iff predictive-info saved $>$ heat-tax cost of the ascent, else overflow decays $=$ forgetting). **Continuity carrier:** the topological bit (`TWO BITS`, free to hold, gauge-irremovable) threads identity through the duty cycle — recurse $=$ keep a protected-current *generator* that re-renders the amplitude state on waking (non-interconvertibility forbids storing the *state* in topology: persist the generator, not the output), forget $=$ no generator persists so amplitude decays; `frustration-ascent` is what builds these generators up the tower. Imports read as "character recurs," not new machinery: Wilsonian RG, Mori–Zwanzig ($\Pi_{\text{slow}}$), Still/Bialek predictive-info bound, Betti/cycle-rank. **Scopings (both load-bearing):** the duty-cycle iff is *endogenous*-only (an external clock duty-cycles without the mechanism — excluded); the recurse-forget threshold is sharp only at rate-distortion-optimal encoding (P4/P5 — softens to a band off the optimum). · **↑** a runnable spec on a *named $\beta_{\text{mem}}<1$ substrate*: fan-in saturation + nesting demonstrated, the coarse-grained level's kernel form measured forced-not-fitted → `battery`. · **✗** a substrate forcing a sparsification hierarchy whose coarse-grained level **fails the two-mode-kernel form** (no $c/s/r$, different FDR) → kills RG-closure for $\beta_{\text{mem}}<1$, demotes the tower to heat-tax bookkeeping; OR an *endogenous* duty-cycler lacking (a)+(b); OR a deep-tower contended substrate that never duty-cycles. · **→** receipts §RG closure (the $\beta_{\text{mem}}<1$ / per-class $\beta$-function open item this sharpens) + §6.5 meta-ledger flow / §6 heat-tax / §22 priority / §17 information; **gated** on the $\langle\sigma\rangle$ apparatus + a real $\beta_{\text{mem}}<1$ substrate (the synthetic two-mode kernel is dynamically flat — cannot host this test).
* `frustration-ascent` **[sharpening]** — *verdict:* the **b₁-growth** companion to `consolidation-ascent`. **b₁-growth leg INSTANCED at calibration grade** (`mpa-conform/scripts/frustration_ascent.py`: an exact nested-collective OU tower platforms +1 protected cycle per frustrated ascent; coarse coupling $=A_{\text{CYC}}$ again $=$ structural type-identity; reciprocal bond $=+0$, kill clean) — but the meta-frustration was **hand-supplied** ($A_{\text{CYC}}$ drawn in each level) $=$ *fitted* $=$ calibration, not vindication. This MECHANISM claim's promote path is a **forced-not-fitted derivation**: does a meta-triad EMERGE from a provably **even-parity** (symmetric) coupling of internally-frustrated subs, with **no** odd-parity meta-bond drawn? **Tested 2026-05-29** (`chiral_bonding.py`, `chiral_selffield.py`, `chiral_tilt.py`, `tilt_rescue.py`): **forced-not-fitted but CONDITIONAL.** (1) Sub-chirality ALWAYS propagates into the coarse description — Schur-complement antisym $M_{\text{eff}}\propto$ sub-chirality, forced, tracking the closed form $\sqrt3\,g/(\gamma^2{+}3g^2)$, $\to0$ as $g\to0$ (the **no-creation-from-balance** certifier). (2) But it **closes a *protected* meta-cycle only when a collective degeneracy is symmetry-protected**: the seed enters at $O(\kappa^2)$ while a generic even-parity coupling splits the collective levels at $O(\kappa)$ — so the cycle closes for a C3-covariant arena and gives $+0$ for uniform/generic couplings. (3) The field's **orientation is self-generated**: three subs whose *chimeric normal* (the $(1,1,1)$ axis, generically $=$ collective real mode $=$ chirality axis) aligns to its own ensemble mean spontaneously commits to a homochiral handedness above a **Curie ignition Wall** (c/s/r), which closes the cycle. (4) The closed cycle is **brittle to generic normal-tilt**: dies at $\theta_c\approx6°$ worst-case / $10°$ median at $\kappa{=}0.3$, scaling $\theta_c\propto\kappa$; the downstream **"pull" rescues** a tilting stream while $\kappa\gtrsim\theta/33°$. **Establishment home (research import 2026-05-29, `docs/character cascade research.md`):** our numbers sit on canonical closed forms — the emergent oscillation is a non-Hermitian **exceptional point** (`pa:nonhermitian-ep`: $\omega^2=\Gamma^2-(\delta/2)^2$, R²=1.00, EP at $\delta=2\Gamma$, onset exponent ½); the tilt-robustness + pull-rescue is the **Arnold tongue / Adler locking range** (`pa:adler-locking`: $\theta_c\propto\kappa$); the self-field SSB is `pa:frank-autocatalysis`/`pa:kondepudi` (Landau pitchfork, $K_c=c$, $\beta=½$); the chiral-mode elimination is `pa:slaving`/`pa:mz-projection` ∘ `pa:nonreciprocal-transition`. The ~39° chirality sign-flip node is model-specific (Berry would predict 60° — no clean solid-angle match) → open. MPA adds the *reading* (recursion-cascade instance), no new object. **Corrected reframe (supersedes the old "balanced cluster ejects current upward / parasitic→generative / stop living downstream"):** no-creation-from-balance forbids ejecting protected current from a balanced cluster — frustration must be **supplied**, then it nests. Defensible layer-2 grade: **generative-of-description-and-orientation, parasitic-on-substrate** — the cascade mints the coarse register and self-lights the field's orientation, but the substrate must supply THREE things: a drive $+$ a frustrable sub-topology $+$ a **rigid meta-scale symmetry** the deformations stay inside. **trigger (I4):** the four runnable specs exist + the forced-not-fitted derivation is instanced (conditional) — steeping→sharpening. · **↑** a **real symmetric chiral substrate** (homochirality network / active-solid lattice) where the bootstrap self-lights **and** the meta-cycle survives the substrate's native deformations (tilt within its protecting symmetry) → `battery`/`staked` by forced-not-fitted derivation on a real instance. · **✗** generic (symmetry-broken) substrates never sustain a platformed cycle — the tilt-brittleness is fatal even given a symmetry — OR no real substrate supplies a rigid-enough symmetric arena → the layer-2 generative bet is **bootstrap-blocked** (MPA stays parasitic). · **→** engine `COMPRESSION` (Wall = loss of normal hyperbolicity / RG flow) + `COMPOSITE CATALOGUE` (c–c–c obstructive → $k_{\text{frust}}$) + Thm 9 / `TWO FACES`; complements `consolidation-ascent` (b₁ growth vs b₁-invariant threading); the Curie ignition sharpens `wall-as-type-boundary`. **Recursion (layer 2, I5):** the *structural* side of the generative bet (paired with `flow-resident-number`); this session sharpens it from "generative" to **conditionally generative (orientation + description), parasitic on a substrate-supplied rigid symmetric arena** — the generative window is narrow and symmetry-gated. **Structural side STAKED 2026-05-29** via `staked:character-primitives` (the deformation-algebra closure promoted to engine `DEFORMATION GENERATORS` by the Part C forced-not-fitted derivation — gl(3) Cartan, autonomous closure, conditional symmetry); `frustration-ascent` itself stays `[sharpening]` for the **coverage/instance** side (a real symmetry-protected chiral substrate sustaining the cascade), which the parked syn3 frontier owes.

* `wall-as-type-boundary` **[sharpening]** — *verdict:* **reframed from a single "Wall" to a type-boundary LADDER (2026-05-29, Ron's plateau insight):** the cascade is a sequence of **plateaus** — flow-resident closed platforms, the IR-attracting fixed point $\mathcal{M}_2$ of the level-to-level map $\mathcal{C}$ ($\varepsilon=\|\mathcal{C}\|_{op}<1$ = contraction onto it); the topology occupying the inter-level region is that level's protected $b_1$, held leak-free by the topological bit while the amplitude relaxes onto $\mathcal{M}_2$ — connected by **boundaries** of three kinds, sorted onto the TWO FACES: **(i) ignition** (amplitude face — pitchfork/transcritical $c/s/r$ register-on; the *landing* onto a plateau; **pinned**: Curie $K_c{=}c$, $\beta{=}\tfrac12$); **(ii) topological flip** (sign face — chirality reversal forced through the balanced state, $\ge1\,$ch; the $\sim39°$ holonomy node, model-specific/frontier); **(iii) closure-loss** (amplitude face — $\varepsilon\to1$, leading eigenvalue $\to1$, spectral gap closes, trail-class metric undefined → **loss of normal hyperbolicity**: the reduced plateau description fails). Only (iii) is wall-like; (i) is constructive (the landing, not a barrier), (ii) is the TWO-BITS modification cost. **The plateau (RG type-identity) and ignition (pitchfork) are already `proven`/pinned in the core; (iii) is the sole open conjecture — and the leg `dynamical_tower.py`/`delay_tower.py` could NOT instance** (every physically-natural weak-coupling realization SYNCHRONIZES to a torus, it does not chaos). **Closure-loss scoped CONDITIONAL** (parasitic-on-substrate, the Part-C strict-K3 move): *given* the amplitude face driven to $\varepsilon\to1$ with the §14 delay-Hopf mechanism active ($K>\gamma_s$, delayed negative self-feedback per level), the flow-resident closure is lost and classical chaos fills in — **not forced generically** (the $N\ge3$/Ruelle–Takens framing is suspect: delay-Hopf chaoses at $N{=}1$, so chaos is delay-driven, not platform-count-gated). **trigger (I4):** the plateau-ladder formalization — the boundary taxonomy is a concrete mechanism, the pre/post observable a way to test — steeping→sharpening. **Establishment landing (research 2026-05-29, three-model convergence, `docs/ladder research and prompt.md`):** rigorous home = a **hierarchy of normally hyperbolic invariant manifolds (NHIM)** (Fenichel / Hirsch–Pugh–Shub) — the $\varepsilon<1$ contraction IS the normal-hyperbolicity spectral-gap condition, each plateau a persistent NHIM carrying the universal normal form (phenomenological twin: Haken slaving; **NOT** Wilsonian RG, which flows *between* fixed points); the preserved topological invariant ↔ topological-phase / persistent-homology survival. **Correction (unanimous):** boundary (iii) is **loss of normal hyperbolicity** — gap-closure is *necessary but NOT sufficient* for chaos (post-boundary outcomes: torus bifurcation / intermittency / crisis / chaos / higher-D quasiperiodicity). The 'N≥3 3-torus ⇒ forced chaos' framing is **refuted**: chaos is **delay-driven at N=1** (`delay-hopf`: Mackey–Glass/Farmer), the golden-ratio 3-torus is **robust** at weak coupling (`dissipative-kam`: Grebogi–Ott–Yorke 1985, Broer–Huitema–Takens) — vindicating the `dynamical_tower` honest-negative. **⇒ fired the engine 'Forced chaos' / 'Wall-forces-chaos' claim; corrected to engine `Wall = loss of normal hyperbolicity` (forward-only; lineage in receipts §Wall — chaos mechanism). Root-cause codified: membership self-test (iii) modality-faithful binding + frontier I6.** · **↑** a runnable **CLV diagnostic** (`clv`: minimum angle $\theta_{\min}$ between covariant-Lyapunov-vector subspaces, Ginelli 2007; + smallest transverse exponent $\lambda_\perp$) cleanly separating plateau ($\theta_{\min}, |\lambda_\perp|$ bounded away from 0 — *including* on a normally-hyperbolic torus, no FTLE false-positive) from boundary ($\theta_{\min}\to0$ / $\lambda_\perp\to0$ = loss of normal hyperbolicity) on a cascade substrate → `battery`. **BUILT + validated 2026-05-29 (`mpa-conform/scripts/clv_diagnostic.py`):** $\theta_{\min}$ separates Rössler chaos (min $0.6°\!\to\!0$, tangency) from the coupled-SL 3-torus and synchronized limit cycle (the cascade's own dynamics; min $>82°$, bounded); the robust torus (the `dynamical_tower` honest-negative) is **certified a genuine NHIM plateau** — its leading FTLE carries a positive tail (95th-pct $+0.08$) that $\theta_{\min}$ correctly ignores (the false-positive control). Plateau side validated on the cascade; boundary side on a canonical chaos control (a cascade reaches loss-of-NHIM only in the deferred delay-driven regime). Observable condition met; **operationalized by `battery:wall-ladder` [battery] (planted 2026-05-29)** — the plateau-ladder falsifier of record, runnable now; real-cascade vindicate/kill pending. · **✗** the taxonomy collapses ($\theta_{\min}$ fails to separate plateau from boundary), OR the conditional closure-loss mechanism is incoherent (no regime where it fires). · **→** engine `COMPRESSION` (Wall as loss of normal hyperbolicity / bifurcation of the meta-ledger flow at $\varepsilon{=}1$) + §14 attractor classification; sharpens `consolidation-ascent` / `frustration-ascent` (the plateau ladder = the tower); receipts §14, §RG closure; prior-art `fenichel-nhim`, `clv`, `dissipative-kam`, `center-manifold`, `functional-rg`, `bifurcation-normal-forms`, `nrt-chaos`, `delay-hopf`.

### BATTERY — executable falsifier specs (committed protocols; the runnable face of the core's kill conditions)

* `battery:seam-collapse` **[battery]** — *verdict:* amplitude/drive-seam scaling-collapse battery; runnable on synthetic now, the *forcing* needs a real substrate with a measurable FDR. Target invariant is $(\alpha,F)$, not a coefficient series. · **↑** stable $\alpha$ + universal collapse + substrate-family profile classes on a real substrate → promotes `thm9-crossover`. · **✗** width saturates as $D\to\infty$ (hard seam → asymptotic closure damaged); or no single $\alpha$; or two substrates with equal $(\alpha,w)$ but incompatible profiles (universality-class kill). Run the drive seam ($\gamma\approx D$) and switching seam ($\gamma\approx0$) **independently** — never conflate. · **→** tests receipts §Deformation calculus Thm 9 + engine `ASYMP` (soft-seam; Boolean $=w(D)\to0$).
* `battery:sign-interior` **[battery]** — *verdict:* sign-topological interior-circulation killshot; **interior measurement only** — finite operating point, never the boundary (chit$\to0$ / $\mathcal{A}=0$ / trails$\to0$, never attained). Read the binary chimeric sign $\mathrm{sign}(\mathcal{A})$, not the graded triality. Synthetic internal-consistency check (IK1) runnable now. **RV1 VINDICATED 2026-05-30 on a real emergent substrate — rock-paper-scissors** (cyclic May-Leonard, `mpa-conform/scripts/rps_triad.py`): a C3-symmetric substrate carries a protected chimeric sign on a directed 3-cycle (complex-pair focus, $\mathcal{A}\ne0$), drive-independent (sign invariant across the demographic-noise sweep, $|J|\propto\sigma^2\to0$ as drive→0), flipping only by rewiring; the achiral $\alpha{=}\beta$ boundary is never visited; the symmetric null has no triad and no protected circulation (RK1 does not fire). **⇒ promoted `central-commitment` + `two-frame-construction` (staked→core).** · **↑** RV1 — a protected chimeric sign on a real substrate always sits on a chimeric triad (homochirality = handedness read directly, holding as drive varies over a finite range) → promotes the central commitment **[FIRED → promoted 2026-05-30, RPS]**. · **✗** RK1 — a real substrate with a protected chimeric sign and **no** chimeric triad (kills the central commitment); IK1 — the synthetic sign flips under finite drive variation without rewiring (topological-bit claim internally false). The $\ge 1\,\text{ch}$ flip-floor is an **entailed theorem, not a measurement** — do not visit the zero to establish it. · **→** tests engine `TRIAD/COMMIT` + `BITS`; receipts §Central commitment, §Two bits, §Homochirality.

* `battery:wall-ladder` **[battery]** — *verdict:* the **plateau-ladder** falsifier — a cascade's levels are normally hyperbolic invariant manifolds (NHIM plateaus) carrying the universal normal form, separated by the three boundary kinds (ignition pitchfork / topological flip / closure-loss $=$ loss of normal hyperbolicity). Discriminator $=$ the **CLV minimum angle** $\theta_{\min}$ (`clv`, Ginelli 2007) $+$ smallest transverse exponent $\lambda_\perp$: a plateau holds $\theta_{\min}$ bounded from 0 (Oseledets splitting transverse — *including* a normally-hyperbolic torus, where FTLE false-positives and $\theta_{\min}$ does not); a closure-loss boundary drives $\theta_{\min}\to0$ / $\lambda_\perp\to0$. Runnable: `mpa-conform/scripts/clv_diagnostic.py` — validated synthetically (Rössler chaos $\theta_{\min}\,0.6°\!\to\!0$; coupled-SL 3-torus $83.6°$ + limit cycle $82.8°$ bounded, the cascade's own dynamics; the `dynamical_tower` torus certified NHIM against its positive-FTLE tail). · **↑** on a **real cascade substrate**: its levels read as NHIM plateaus under $\theta_{\min}$ (bounded), the inter-level transitions as the named boundaries, and a level driven to $\varepsilon\to1$ shows $\theta_{\min}\to0$ (closure-loss) → promotes the `wall-as-type-boundary` plateau-ladder. · **✗** $\theta_{\min}$ fails to discriminate — a genuine NHIM plateau reads $\theta_{\min}\to0$, OR a loss-of-NHIM boundary holds $\theta_{\min}$ bounded; OR a real cascade level is **not** an NHIM (no universal-normal-form plateau). · **→** promotes `wall-as-type-boundary`; engine `COMPRESSION` (Wall = loss of normal hyperbolicity) + §14 attractor classification; receipts §Wall — chaos mechanism; prior-art `fenichel-nhim`, `clv`, `dissipative-kam`, `nrt-chaos`, `delay-hopf`.

### STAKED — planted in the core, un-instanced (the bridge rung; mirrors the receipts `staked` tag — I1)

* `staked:two-bits` **[staked]** — *verdict:* one bit per deformation face; the topological bit is free to hold, $\ge 1\,\text{ch}$ per protected sign to modify. · **↑** the floor-isolation protocol run on a real frustrated-and-noisy substrate (split the $1\,\text{ch}$ floor from rewiring work). · **✗** the orthogonality falsifier fires (protected sign carries a duration-scaling maintenance cost, or flips on a continuous sub-$1\,\text{ch}$ path). · **→** receipts §Two bits / engine `BITS`.
* `staked:homochirality` **[staked]** — *verdict:* biological homochirality = the topological bit on a real, ancient substrate; the named real-substrate stake. · **↑** drive-sweep on a real chiral proofreading network: magnitude $\to$ racemic while sign invariant. · **✗** handedness flips under drive titration, or any of the three nulls (balanced network / 2-component bistable / detailed balance). · **→** receipts §Homochirality.
* `staked:gfdr-two-step` **[staked]** — *verdict:* $s$-regime FDR is two-step; short-lag $X{=}1$ is not $r$. · **↑** a **second** instance — recurrence of the short-lag-$X{=}1\leftrightarrow r$ confusion, or a real glass/quantum/brain substrate exercising the two-step (defense ⇒ needs the second instance per the gate). · **✗** a substrate where short-lag $X{=}1$ genuinely *is* $r$. · **→** receipts §gFDR two-step.
* `staked:auto-tuning` **[staked]** — *verdict:* substrate $\gamma_{s,i}$ auto-tunes to the diagonally-stabilising $W=\mathrm{diag}(\gamma_{\text{ref}}/\gamma_{s,i})$. · **↑** a substrate requiring diagonal stability shown to auto-tune to the inverse-form $W$. · **✗** a stable holding on a non-PSD frustration-free topology whose measured $\gamma_{s,i}$ give an indefinite $W\gamma+\gamma^TW$. · **→** receipts §20 auto-tuning.
* `staked:memory-collapse` **[staked]** — *verdict:* $\beta_{\text{mem}}\approx1-\varepsilon$ near the Wall. · **↑** $\beta_{\text{mem}}(\varepsilon)$ measured near the Wall on a real hierarchical substrate, linear with both endpoints respected. · **✗** $\beta_{\text{mem}}$ departs from $1-\varepsilon$. · **→** receipts §9 memory collapse.
* `staked:character-primitives` **[staked]** — *verdict:* the character **deformation-generator basis** is the **Cartan decomposition** of the linear drift, $\mathfrak{gl}(3,\mathbb{R})=\mathbb{R}I\oplus\mathfrak{so}(3)\oplus\mathrm{Sym}_0$ (damping ⊕ chirality mag+sign+orientation ⊕ splitting/detuning) — **exhaustive** ($1{+}3{+}5{=}9$) and **closed** (Cartan brackets; commutator residual $3\!\cdot\!10^{-16}$); + the named-closed extensions (noise/Lyapunov, nonlinear/normal-forms, Floquet, Schur-composition). Closure is **autonomous** (needs no symmetry); the meta-scale protecting symmetry is scoped **conditional** (strict-K3, route (b)): a substrate boundary condition suppressing the $O(\kappa)$ $\mathrm{Sym}_0$ doublet-splitting channel so the $O(\kappa^2)$ $\mathfrak{so}(3)$ seed lifts the degeneracy into a protected counter-rotating meta-cycle (the complex branch of the EP normal form). Planted by the forced-not-fitted derivation (Part C), un-instanced on real substrates by design. **trigger (I4):** `battery:character-primitives` spec drafted (sharpening→battery) + the closure/lift derivation done & verified (`mpa-conform/scripts/character_closure.py`, `mpa-atlas/docs/character_closure_derivation.md`) — battery→staked. · **↑** a **real symmetric chiral substrate** whose native character composes from these generators with these forced responses, the protecting symmetry substrate-supplied (homochirality net / active-solid lattice) → `promoted` (crosses fully — the coverage instance the parked syn3 frontier owes). · **✗ (K1)** a triad deformation not a composition of the generators; **(K2)** a non-model-specific response that misses its canonical form — the $\sim39°$ symmetric-cone node and the non-uniform-squeeze secondary tongues are **quarantined here as model-specific (stay frontier, do NOT promote)**; **(K3)** the protecting symmetry only hand-suppliable (neither derived nor honestly conditioned); **(K4)** I5 fail (an inert constant in any response, or the basis asserted not entailed). · **→** engine `DEFORMATION GENERATORS` + `TWO FACES` / `TWO BITS`; receipts §deformation-generators; prior-art `nonhermitian-ep`, `adler-locking`, `bifurcation-normal-forms`, `nonreciprocal-transition`, `kuramoto`, `slaving`, `mz-projection`, `frank-autocatalysis`, `kondepudi`, `sde-lyapunov`. Operationalizes the structural side of `frustration-ascent`.

---

## TOMBSTONES — discarded false starts (kept so they are not re-explored)

* `flip-cost-across-zero` — protection-test-as-flip-cost across the sign-erased zero. **Died:** formulating protection as a measurement that visits $\mathcal{A}=0$ is malformed by asymptotic closure (the zero is never attained). **Replaced by:** the interior sign-stability test (`battery:sign-interior`); the $\ge 1\,\text{ch}$ floor demoted to an entailed theorem.
* `killshot-bisection` — the "can-kill-can't-vindicate" standdown that split the killshot into a kill-only test. **Died:** it was a boundary-gated formulation; character is degenerate at the boundary, rich in the interior. **Replaced by:** the interior, can-kill-and-vindicate `battery:sign-interior`.
* `thm9-coefficient-series` — Thm 9 bound→equality recovered as an analytic $1/D$ coefficient series (the retired "Test B"). **Died:** two independent passes converged that the seam is a crossover scaling function, not a coefficient series; the series stayed fitted-not-forced. **Replaced by:** the scaling-collapse target $(\alpha,F)$ (`thm9-crossover` + `battery:seam-collapse`).
* `alignment-dependent-beta-alpha` — the $r/(1-r)$ alignment-dependent form for $\beta/\alpha$. **Died:** it captures only the flocked-state correction, not the leading scaling. **Replaced by:** the alignment-independent fingerprint $\beta/\alpha\sim v_0^2\tau_R/D_{\text{trans}}$ (receipts §21).
* `quadratic-lyapunov-candidate` — the $V=\sum_i[-(G_{0,i}-L_i)x_i+\tfrac12\sum_j\gamma_{ij}x_ix_j+L_ix_i\ln(x_i/x_i^*)]$ Lyapunov form. **Died:** fails the critical-point test ($\partial V/\partial x_i|_{x^*}=L_i\ne0$). **Replaced by:** the relative-entropy form (receipts §20).
